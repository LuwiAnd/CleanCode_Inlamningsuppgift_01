using static Inlämningsuppgift_1.Services.Implementations.ProductService;

namespace Inlämningsuppgift_1
{
    public class Tmp
    {
    //    var hasProductInCart = userCart!.CartItems?.FirstOrDefault(ci => ci.ProductId == productId);
    //        if (hasProductInCart == false) userCart!.CartItems?.Add(new CartItem { ProductId = productId, Quantity = quantity
    //});
    //        else hasProductInCart.Quantity += quantity;
    }
}
